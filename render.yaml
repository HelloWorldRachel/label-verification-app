services:
  - type: web
    name: label-backend
    env: docker           # CHANGED: from 'python' to 'docker'
    region: oregon
    plan: free
    rootDir: backend      # ADDED: Tells Render to look inside the backend folder
    envVars:
      - key: PORT
        value: 10000